/* This is the module specification for the Introduce Delay Utility module.
 */

mod_name		Utility;
qualifier		Delay;
typedef struct {

	NameSpecifier	mode;
	double	initialDelay;
	double	finalDelay;

} Delay2;
